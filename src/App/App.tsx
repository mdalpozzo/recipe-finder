import * as React from 'react';
import {
} from 'react-native';

import Navigator from '@Navigator';



const App: React.FC<{}> = ({}): React.ReactElement | null => {
    return (
        <Navigator />
    );
};

export default App;
